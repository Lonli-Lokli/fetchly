{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Next.js: debug server-side",
      "type": "node-terminal",
      "request": "launch",
      "cwd": "${workspaceFolder}/apps/fetchly",
      "command": "cd ../..; npx cross-env NODE_OPTIONS='--inspect' npx nx run fetchly:start"      
    }
  ]
}
